# SEM2 (React)

###

- Есть компонент `Header` и он показывает название магазина (`src/components/Header.tsx`)
- Есть компонент `ProductList` и он отображает список товаров (`src/components/ProductList.tsx`)
- Есть компонент `Footer` с контактной инфой (`src/components/Footer.tsx`)
- Есть главный компонент, который объединяет все три (`src/components/Main.tsx`)
- `App.tsx` подключает главный компонент (`src/App.tsx`)

